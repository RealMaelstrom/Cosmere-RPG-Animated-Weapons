import { RunAnimation } from "./animation-macros";
import { DetectMapping, GetMapping, SyncMapping } from "./mapping";

export var AnimationMacros = {
	detectMapping: DetectMapping,
	syncMapping: SyncMapping,
	getMapping: GetMapping,
	runAnimation: RunAnimation,
}
